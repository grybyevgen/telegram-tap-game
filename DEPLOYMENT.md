# Инструкция по деплою и тестированию Telegram Mini App "Тапалка"

## Содержание
1. [Подготовка проекта](#подготовка-проекта)
2. [Деплой на хостинг](#деплой-на-хостинг)
3. [Настройка Firebase](#настройка-firebase)
4. [Создание Telegram бота](#создание-telegram-бота)
5. [Тестирование](#тестирование)
6. [Автоматический деплой](#автоматический-деплой)

---

## Подготовка проекта

### 1. Проверка файлов

Убедитесь, что все файлы на месте:

```
tap-game/
├── index.html
├── style.css
├── script.js
├── firebase-config.js
├── manifest.json
├── icon-192x192.png (или SVG)
├── icon-512x512.png (или SVG)
└── .env (НЕ коммитьте в git!)
```

### 2. Создание .env файла

```bash
# Скопируйте пример
cp env.example .env

# Отредактируйте .env и заполните реальные значения Firebase
```

### 3. Создание иконок

Если у вас еще нет PNG иконок:

1. Откройте `generate-icons.html` в браузере
2. Настройте параметры
3. Скачайте `icon-192x192.png` и `icon-512x512.png`
4. Поместите файлы в корень проекта

Или используйте Node.js:

```bash
npm install sharp
node create-icons.js
```

### 4. Обновление Firebase конфигурации

Откройте `firebase-config.js` и обновите функцию `getFirebaseConfig()`:

```javascript
function getFirebaseConfig() {
    return {
        apiKey: "ваш_api_key",
        authDomain: "ваш_проект.firebaseapp.com",
        projectId: "ваш_проект_id",
        storageBucket: "ваш_проект.appspot.com",
        messagingSenderId: "ваш_sender_id",
        appId: "ваш_app_id"
    };
}
```

---

## Деплой на хостинг

### Вариант 1: GitHub Pages (Бесплатно, просто)

#### Шаг 1: Создание репозитория

```bash
# Инициализация git (если еще не инициализирован)
git init

# Добавление файлов
git add .

# Первый коммит
git commit -m "Initial commit: Tap Game"

# Создайте репозиторий на GitHub, затем:
git remote add origin https://github.com/ваш_username/tap-game.git
git branch -M main
git push -u origin main
```

#### Шаг 2: Настройка GitHub Pages

1. Перейдите в Settings → Pages
2. Source: выберите "Deploy from a branch"
3. Branch: `main` / `root`
4. Нажмите Save

#### Шаг 3: Проверка деплоя

Ваше приложение будет доступно по адресу:
`https://ваш_username.github.io/tap-game/`

**⚠️ ВАЖНО:** GitHub Pages работает только со статическими файлами. Для Firebase переменных окружения используйте прямой метод конфигурации в `firebase-config.js`.

### Вариант 2: Netlify (Рекомендуется для Firebase)

#### Шаг 1: Установка Netlify CLI

```bash
npm install -g netlify-cli
```

#### Шаг 2: Деплой через CLI

```bash
# Логин
netlify login

# Инициализация проекта
netlify init

# Деплой
netlify deploy --prod
```

Или через веб-интерфейс:

1. Зарегистрируйтесь на [netlify.com](https://netlify.com)
2. Перетащите папку `tap-game` в Netlify
3. Или подключите GitHub репозиторий

#### Шаг 3: Настройка переменных окружения (опционально)

В Netlify Dashboard:
1. Site settings → Environment variables
2. Добавьте переменные с префиксом `VITE_` (если используете Vite)

### Вариант 3: Vercel

#### Шаг 1: Установка Vercel CLI

```bash
npm install -g vercel
```

#### Шаг 2: Деплой

```bash
vercel
```

Или через веб-интерфейс:

1. Зарегистрируйтесь на [vercel.com](https://vercel.com)
2. Import проект из GitHub
3. Настройте переменные окружения в Settings → Environment Variables

### Вариант 4: Firebase Hosting

#### Шаг 1: Установка Firebase CLI

```bash
npm install -g firebase-tools
```

#### Шаг 2: Инициализация

```bash
firebase login
firebase init hosting
```

Выберите:
- Public directory: `tap-game` (или `.` если уже в папке)
- Single-page app: `Yes`
- GitHub Actions: `Yes` (опционально)

#### Шаг 3: Деплой

```bash
firebase deploy --only hosting
```

---

## Настройка Firebase

### Шаг 1: Создание проекта

1. Перейдите на [Firebase Console](https://console.firebase.google.com/)
2. Нажмите "Add project"
3. Введите название: `tap-game` (или любое другое)
4. Отключите Google Analytics (опционально)
5. Нажмите "Create project"

### Шаг 2: Добавление веб-приложения

1. В Firebase Console нажмите на иконку Web (`</>`)
2. Зарегистрируйте приложение:
   - **App nickname**: Тапалка
   - **Firebase Hosting**: можно настроить позже
3. Скопируйте конфигурацию Firebase

### Шаг 3: Настройка Firestore Database

1. В Firebase Console перейдите в **Firestore Database**
2. Нажмите "Create database"
3. Выберите режим:
   - **Production mode** (рекомендуется)
   - **Test mode** (для разработки, отключится через 30 дней)
4. Выберите регион (например, `europe-west`)

### Шаг 4: Правила безопасности Firestore

В Firestore Database → Rules вставьте:

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // Коллекция пользователей
    match /users/{userId} {
      // Чтение: публичное (для рейтинга)
      allow read: if true;
      
      // Создание: только свой документ
      allow create: if request.resource.data.userId == userId;
      
      // Обновление: только свой документ
      allow update: if request.resource.data.userId == userId 
                   && resource.data.userId == userId;
      
      // Удаление: запрещено
      allow delete: if false;
    }
    
    // Коллекция рефералов
    match /referrals/{referralCode} {
      // Чтение: публичное (для проверки реферальных кодов)
      allow read: if true;
      
      // Запись: разрешено для создания новых реферальных кодов
      allow write: if true;
      
      // ВНИМАНИЕ: Для продакшена рекомендуется добавить более строгие правила
      // с проверкой Telegram WebApp данных
    }
  }
}
```

**⚠️ ВНИМАНИЕ:** Эти правила разрешают публичную запись для простоты. Для продакшена добавьте проверку через Telegram WebApp данные!

### Шаг 5: Создание индексов

Для работы рейтинга создайте индексы:

1. Firestore Database → Indexes → Create Index
2. **Collection ID**: `users`
3. **Fields to index**: 
   - `coins` (Descending)
4. **Query scope**: Collection
5. Нажмите "Create"

Создайте также индекс для `totalTaps` (Descending)

### Шаг 6: Обновление конфигурации в проекте

Обновите `firebase-config.js` с вашими данными:

```javascript
function getFirebaseConfig() {
    return {
        apiKey: "AIzaSy...", // Из Firebase Console
        authDomain: "your-project.firebaseapp.com",
        projectId: "your-project-id",
        storageBucket: "your-project.appspot.com",
        messagingSenderId: "123456789",
        appId: "1:123456789:web:abc123"
    };
}
```

---

## Создание Telegram бота

### Шаг 1: Создание бота через @BotFather

1. Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Введите название бота: `Тапалка Игра`
4. Введите username бота: `your_tap_game_bot` (должен заканчиваться на `bot`)
5. Скопируйте **Bot Token** (он понадобится позже)

### Шаг 2: Настройка Mini App

1. Отправьте `/newapp` боту @BotFather
2. Выберите вашего бота из списка
3. Заполните данные:
   - **Title**: Тапалка
   - **Description**: Кликер-игра с реферальной системой
   - **Photo**: Загрузите иконку игры (512x512 PNG)
   - **Short name**: tap_game (без пробелов и спецсимволов)
   - **Web App URL**: `https://your-domain.com` (URL вашего деплоя)
   - **GIF**: опционально
   - **GIF as cover**: опционально

4. Сохраните изменения

### Шаг 3: Привязка кнопки к боту

Для добавления кнопки "Играть" к вашему боту:

1. Отправьте `/mybots` в @BotFather
2. Выберите вашего бота
3. Bot Settings → Menu Button
4. Выберите "Configure menu button"
5. Введите текст кнопки: `🎮 Играть`
6. Введите URL: `https://your-domain.com`

### Шаг 4: Получение Bot Token

Сохраните Bot Token, который вы получили при создании бота. Он выглядит так:
```
1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
```

---

## Тестирование

### Тест 1: Проверка в браузере

1. Откройте URL вашего деплоя в браузере
2. Откройте DevTools (F12)
3. Проверьте консоль на ошибки
4. Убедитесь, что:
   - ✅ Страница загружается
   - ✅ Нет ошибок JavaScript
   - ✅ Firebase инициализируется (если настроен)
   - ✅ Иконки загружаются
   - ✅ Manifest работает

### Тест 2: Тестирование в Telegram через @WebAppTestBot

1. Найдите бота [@WebAppTestBot](https://t.me/WebAppTestBot) в Telegram
2. Отправьте команду `/start`
3. Отправьте URL вашего приложения: `https://your-domain.com`
4. Проверьте работу:
   - ✅ Приложение открывается
   - ✅ Telegram WebApp SDK загружается
   - ✅ Данные пользователя получаются
   - ✅ Кнопка тапа работает
   - ✅ Монеты начисляются

### Тест 3: Тестирование через ваш бот

1. Найдите вашего бота в Telegram
2. Нажмите кнопку "🎮 Играть" (Menu Button)
3. Проверьте все функции:
   - ✅ Тап и начисление монет
   - ✅ Покупка улучшений
   - ✅ Реферальные ссылки
   - ✅ Сохранение прогресса

### Тест 4: Тестирование реферальных ссылок

1. Скопируйте реферальную ссылку из игры
2. Откройте ссылку в браузере: `https://your-domain.com?ref=TAP12345678`
3. Убедитесь, что:
   - ✅ Реферальный код определяется из URL
   - ✅ Информация о реферере сохраняется
   - ✅ Бонус начисляется при первом тапе

**Тест в Telegram:**
1. Откройте реферальную ссылку через вашего бота
2. Проверьте, что реферальный код обрабатывается
3. Сделайте первый тап
4. Проверьте, что реферер получил бонус

### Тест 5: Проверка Firebase

1. Откройте Firebase Console → Firestore Database
2. Проверьте коллекции:
   - ✅ `users` - создаются документы пользователей
   - ✅ `referrals` - создаются реферальные коды
3. Проверьте данные:
   - ✅ Прогресс сохраняется
   - ✅ Реферальные связи создаются
   - ✅ Статистика обновляется

### Тест 6: Проверка PWA

1. Откройте приложение в браузере (Chrome/Edge)
2. Откройте DevTools (F12) → Application
3. Проверьте:
   - ✅ Manifest загружается
   - ✅ Service Worker (если настроен)
   - ✅ Иконки доступны
   - ✅ Offline поддержка (если настроена)

---

## Автоматический деплой

### GitHub Actions (Автоматический деплой)

Файл `.github/workflows/deploy.yml` настроен для автоматического деплоя на GitHub Pages при каждом push в ветку `main` или `master`.

**Настройка:**
1. Перейдите в Settings → Pages вашего репозитория
2. Source: выберите "GitHub Actions"
3. При каждом push приложение будет автоматически деплоиться

### Скрипты для деплоя

#### Linux/Mac: deploy.sh

```bash
# Сделать исполняемым (только первый раз)
chmod +x deploy.sh

# Деплой на GitHub Pages
./deploy.sh github

# Деплой на Netlify
./deploy.sh netlify

# Деплой на Vercel
./deploy.sh vercel

# Деплой на Firebase
./deploy.sh firebase
```

#### Windows: deploy.bat

```batch
REM Деплой на GitHub Pages
deploy.bat github

REM Деплой на Netlify
deploy.bat netlify

REM Деплой на Vercel
deploy.bat vercel

REM Деплой на Firebase
deploy.bat firebase
```

### Ручной деплой через CLI

#### Netlify CLI

```bash
# Установка
npm install -g netlify-cli

# Логин
netlify login

# Инициализация (первый раз)
netlify init

# Деплой
netlify deploy --prod
```

#### Vercel CLI

```bash
# Установка
npm install -g vercel

# Деплой
vercel

# Продакшн деплой
vercel --prod
```

#### Firebase CLI

```bash
# Установка
npm install -g firebase-tools

# Логин
firebase login

# Инициализация (первый раз)
firebase init hosting

# Деплой
firebase deploy --only hosting
```

---

## Типичные проблемы и решения

См. файл `TROUBLESHOOTING.md` для подробной информации о решении проблем.

### Быстрые решения:

**Проблема:** Приложение не открывается в Telegram
- ✅ Проверьте HTTPS (Telegram требует HTTPS)
- ✅ Проверьте URL в настройках бота
- ✅ Убедитесь, что URL доступен публично

**Проблема:** Firebase не инициализируется
- ✅ Проверьте конфигурацию в `firebase-config.js`
- ✅ Проверьте правила Firestore
- ✅ Проверьте консоль браузера на ошибки

**Проблема:** Реферальные ссылки не работают
- ✅ Проверьте формат кода в URL
- ✅ Проверьте правила Firestore для коллекции `referrals`
- ✅ Проверьте логи в консоли

**Проблема:** Иконки не отображаются
- ✅ Убедитесь, что файлы `icon-192x192.png` и `icon-512x512.png` существуют
- ✅ Проверьте пути в `manifest.json`
- ✅ Проверьте MIME-типы на сервере

---

## Чеклист перед запуском

- [ ] Все файлы проекта готовы
- [ ] Firebase проект создан и настроен
- [ ] Правила Firestore настроены
- [ ] Индексы Firestore созданы
- [ ] Конфигурация Firebase обновлена в коде
- [ ] Иконки созданы и размещены
- [ ] Приложение задеплоено на хостинг
- [ ] URL доступен по HTTPS
- [ ] Telegram бот создан через @BotFather
- [ ] Mini App настроен в @BotFather
- [ ] URL приложения указан в настройках бота
- [ ] Тестирование пройдено
- [ ] Реферальные ссылки работают

---

## Дополнительные ресурсы

- [Telegram Mini Apps Documentation](https://core.telegram.org/bots/webapps)
- [Firebase Documentation](https://firebase.google.com/docs)
- [GitHub Pages Documentation](https://docs.github.com/en/pages)
- [Netlify Documentation](https://docs.netlify.com/)

---

## Поддержка

Если возникли проблемы, проверьте:
1. Консоль браузера (F12)
2. Логи Firebase Console
3. Файл `TROUBLESHOOTING.md`
4. GitHub Issues (если используете GitHub)
